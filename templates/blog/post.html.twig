{% extends 'base.html.twig' %}
{% block title %}{{ post.title }}{% endblock %}

{% block body %}

        <p>{{ id }}</p>
        <p>{{ post.title }}</p>
        <p>{{ post.text }}</p>
        <p>{{ post.date|date('H:i:s') }}</p>
{% endblock %}